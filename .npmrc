tag-version-prefix=""
# vitest@1 peer-requires @types/node >= 18, but this project pins ^16 to match
# Obsidian's Electron runtime. The conflict is benign — vitest runs at test time
# only and is not bundled into main.js — so allow the install to proceed.
legacy-peer-deps=true